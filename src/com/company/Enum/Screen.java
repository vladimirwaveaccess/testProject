package com.company.Enum;

public enum Screen {
/*    UpStart(""),
    Program(LOCALE.getLocale().PROGRAMMING()),
//    ...
    Start(LOCALE.getLocale().DEVICE_TITLE());

    private final String name;

    Screen(String name) {
        this.name = name;
    }

    public String getName() {
        return name;
    }

    public static Screen fromText(String text) {
        for (Screen screen : values()) {
            if (screen.getName().equalsIgnoreCase(text)) {
                return screen;
            }
        }
        return null;
    }
 */
}
